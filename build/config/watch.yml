options:
  spawn: false
  livereload: "<%= liveReload %>"

# Core stuff
# ----------
styles:
  files:
    - "libs/**/*.css"
    - "src/styles/*.less"
  tasks: less:core

scripts:
  files:
    - "src/scripts/*.js"
  tasks:
    - jshint:core
    - jscs:core
    - concat:core
    - concat:all

templates:
  files:
    - "src/templates/**/*.html"
  tasks:
    - ngtemplates:core
    - concat:all

fonts:
  files:
    - "src/fonts/*"
  tasks: copy:fonts

unit:
  files:
    - "test/unit/**/*"
  tasks:
    - jshint:tests
    - jscs:tests
    - karma:core

# Docs stuff
# ----------
docsHTML:
  files:
    - "src/docs/**/*.swig"
    - "<%= icons.docs.src %>"
    - "<%= icons.docs.options.categories %>"
    - "build/*.json"
  tasks:
    - icons:docs
    - swig:docs

docsJS:
  files:
    - "assets/scripts/*.js"

docsCSS:
  files:
    - "assets/**/*.less"
    - "<%= watch.styles.files %>"
  tasks: less:docs